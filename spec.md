# 강의 정산 퀘스트 — 기획서

> 연습용 화면입니다. 실제 회사 자료는 쓰지 않고 가짜 예시 데이터만 씁니다.

## 목표

강의 운영 담당자가 **매달 "이번 달에 정산해야 할 건이 뭐가 남았지"를 노션에서 눈으로 훑어 찾는 일**을 줄입니다.

노션 정산내역을 월별로 갈라서, 아직 정산이 끝나지 않은 건만 골라 게임 퀘스트 목록처럼 한 줄씩 보여줍니다. 담당자는 위에서부터 하나씩 처리하며 진행도만 확인하면 됩니다. 표를 정렬하고 필터를 걸고 계산기를 두드리는 단계가 사라집니다.

## 입력

- **정산내역 목록 한 벌** — 화면에 이미 들어 있는 가짜 예시 데이터입니다. 노션에서 내보낸 표를 흉내 낸 모양이고, 한 건은 다음 칸으로 이루어집니다.
  - 강사명 / 강의명(모듈) / 강의일자 / 강의료 / 정산상태(미정산·정산요청·정산완료)
- **버튼 한 번의 누름** — 담당자가 주는 입력은 이것뿐입니다. 값을 고르거나 적어 넣는 칸은 없습니다.

넣지 않는 것: 로그인, 회원 구분, 노션 API 연동, 내용을 적어 저장하는 기능.

## 단계

1. 화면이 열리면 가짜 정산내역이 그대로 놓여 있습니다. 아직 정리되지 않은 상태입니다.
2. 담당자가 버튼 하나(`정산 퀘스트 만들기`)를 누릅니다.
3. 강의일자를 보고 **월별로 묶습니다.** (예: 9월 / 10월)
4. 각 묶음에서 정산상태가 `정산완료`가 **아닌 건만 남깁니다.** 완료된 건은 목록에서 빼고 숫자로만 셉니다.
5. 남은 건을 강의일자가 이른 순서로 줄 세웁니다. 오래 밀린 건이 위로 옵니다.
6. 월마다 남은 건수 · 남은 금액 합계 · 진행도(전체 중 완료 몇 건)를 셉니다.
7. 결과를 같은 화면 아래에 퀘스트 목록으로 그립니다.

## 출력

**결과는 한 종류입니다: 한 장짜리 "월별 정산 퀘스트 목록".**

화면을 옮기지 않고 버튼 바로 아래에 나타납니다.

### 화면에 무엇이 몇 개 보이나 (아래 예시 데이터 기준)

| 덩어리 | 개수 | 내용 |
|---|---|---|
| 머리말 | 글 3줄 | 연습용 안내 1줄 / 제목 1줄 / 설명 1줄 |
| 버튼 | 1개 | `정산 퀘스트 만들기` |
| 전체 요약 | 글 1줄 | `남은 퀘스트 6건 · 3,850,000원` |
| 월 묶음 | 2개 | 9월 / 10월 |
| 퀘스트 줄 | 표 6줄 | 9월 3줄 + 10월 3줄 |

월 묶음 하나 = **요약 글 1줄 + 진행 막대 1개 + 표 1개**입니다.

### 월 묶음 머리 — 글 1줄 + 막대 1개

- 글 1줄: `9월 · 남은 3건 · 1,950,000원 · 5건 중 2건 완료`
- 막대 1개: 그 달 전체 건수 대비 완료 비율을 채웁니다. (5건 중 2건 = 40%)

### 퀘스트 표 — 월 묶음마다 1개, **열 5개**

열 이름은 왼쪽부터 이 순서로 고정입니다.

| 순서 | 열 이름 | 담기는 값 | 정렬 |
|---|---|---|---|
| 1 | 강사명 | 사람 이름 | 왼쪽 |
| 2 | 강의명 | 모듈 번호 + 강의 제목 | 왼쪽 |
| 3 | 강의일자 | `9월 5일 (토)` | 왼쪽 |
| 4 | 강의료 | `700,000원` | 오른쪽, 숫자 폭 고정 |
| 5 | 상태 | 배지 `미정산` 또는 `정산요청` 두 값만 | 왼쪽 |

- 표의 줄 수 = 그 달에 남은 건수입니다. 머리줄(열 이름 줄)은 표마다 1줄, 따로 셉니다.
- `정산완료` 건은 표에 넣지 않습니다. 묶음 머리의 `5건 중 2건 완료`에만 셉니다.
- 상태 배지는 색으로 구분합니다. `미정산`은 눈에 띄게, `정산요청`은 한 단계 옅게 둡니다.
- 남은 게 없는 달은 표 대신 글 1줄만 둡니다: `이 달은 모두 정산 완료`

### 예시 데이터 (지어낸 값)

**9월 — 전체 5건 / 완료 2건 / 남은 3건 / 1,950,000원**

| 강사명 | 강의명 | 강의일자 | 강의료 | 상태 |
|---|---|---|---|---|
| 최유라 | M4 기획자를 위한 금융데이터 활용 실무 | 9월 5일 (토) | 700,000원 | 미정산 |
| 이준호 | M3 리스크 데이터 이해 | 9월 11일 (목) | 600,000원 | 미정산 |
| 한지훈 | M6 금융 규제와 데이터 | 9월 19일 (토) | 650,000원 | 정산요청 |

**10월 — 전체 4건 / 완료 1건 / 남은 3건 / 1,900,000원**

| 강사명 | 강의명 | 강의일자 | 강의료 | 상태 |
|---|---|---|---|---|
| 배수민 | M8 AI로 문서 자동화 | 10월 2일 (목) | 750,000원 | 미정산 |
| 문태경 | M9 데이터 시각화 실무 | 10월 10일 (금) | 550,000원 | 정산요청 |
| 신도윤 | M11 실습 프로젝트 코칭 | 10월 24일 (금) | 600,000원 | 미정산 |

출력은 화면에 그려지는 것으로 끝입니다. 파일로 내려받거나 어딘가에 저장하지 않습니다. 새로고침하면 처음 상태로 돌아갑니다.
